import { LoginForm } from '@/components/dashboard/auth/LoginForm';

export const metadata = {
  title: 'Login - AdminPro',
  description: 'Login to access the Blank Page administration system',
};

export default function LoginPage() {
  return <LoginForm />;
}
